"use client";

/**
 * AnalysisMapWrapper
 * Simple wrapper component - Google Maps API is now loaded globally via GoogleMapsProvider
 */
export default function AnalysisMapWrapper({ children }) {
  return children;
}
